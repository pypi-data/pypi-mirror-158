raise ImportError(
    """This package was installed from pypi.org, but it is a private package.
    This is a placeholder to avoid dependency confusion.
    Please specify an --extra-index-url in your pip command to resolve the package correctly.
    """
)
