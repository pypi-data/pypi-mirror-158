def clipImages(imagePathsArray):
    # Iterate each path
    # Unzip into data directory named after zip
    # Iterate through each .tiff file and clip/mask
    ## This can be achieved through GDAL with warp but rasterio.mask is easier
    return "clippedImage"
