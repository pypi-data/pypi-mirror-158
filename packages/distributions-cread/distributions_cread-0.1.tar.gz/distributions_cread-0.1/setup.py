from setuptools import setup

setup(name='distributions_cread',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_cread'],
      author = 'Connor Read',
      zip_safe=False)
